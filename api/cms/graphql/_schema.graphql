schema {
    query: Query
    mutation: Mutation
}

type Query {
    public: PublicModule
}

type Mutation {
    public: PublicModule
}

type Meta {
    page: Int!
    limit: Int!
    totalRecords: Int!
    totalPages: Int!
}

input Filter {
    page: Int!
    limit: Int!
    sort: String!
    orderBy: String!
}
