schema {
    query: Query
    mutation: Mutation
}

type Query {
    product: Product
}

type Mutation {
    product: Product
}

scalar Time

type Meta {
    page: Int!
    limit: Int!
    totalRecords: Int!
    totalPages: Int!
}

input Filter {
    page: Int!
    limit: Int!
    sort: String!
    orderBy: String!
}
